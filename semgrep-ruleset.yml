# Official Semgrep Registry Packs (Auto-Updated)
# This file eliminates local rules and uses curated, maintained rulepacks.
# It is automatically consumed by semgrep-action.

config:
  - p/ci
  - p/java
  - p/security-audit

# Optional: ignore test and generated files
exclude:
  - 'src/test/**'
  - '**/generated/**'

# Add rule overrides here ONLY if needed
rules: []
